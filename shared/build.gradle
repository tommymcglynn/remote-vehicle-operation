group 'com.mcglynn'
version '1.0-SNAPSHOT'

sourceCompatibility = 1.8

repositories {
    mavenCentral()
}

dependencies {
    compile fileTree(dir: "${opencvDir}/bin", include: "*.jar")
    compile 'io.netty:netty-all:4.1.+'
}
